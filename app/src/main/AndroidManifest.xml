<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/pesowiselogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/pesowiselogo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PisoWiseFinal"
        tools:targetApi="31">

        <!-- Main Dashboard Activity -->
        <activity
            android:name=".ui.dashboards.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.transactions.AddExpensesActivity"
            android:exported="false" />

        <activity
            android:name=".ui.transactions.TransactionActivity"
            android:exported="false" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>
